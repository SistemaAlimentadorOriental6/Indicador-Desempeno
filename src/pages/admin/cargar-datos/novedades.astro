---
import AdminLayout from "@layouts/AdminLayout.astro";
import DataUploadCard from "@components/admin/DataUploadCard.astro";
---

<AdminLayout titulo="Cargue de Novedades">
    <div class="page-container-full">
        <div class="content-section-full">
            <DataUploadCard
                title="Cargue de Novedades"
                description="Sube el archivo Excel con las novedades, ausentismos o factores especiales de los operadores."
                endpoint="carga-datos/novedades"
                icon="nov"
            />
        </div>
    </div>
</AdminLayout>

<style>
    .page-container-full {
        padding: 20px;
        width: 100%;
        animation: fadeIn 0.5s ease-out;
    }

    @keyframes fadeIn {
        from {
            opacity: 0;
            transform: translateY(10px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .content-section-full {
        width: 100%;
        display: flex;
        justify-content: center;
    }
</style>
